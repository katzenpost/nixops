{

### AUTHORITY

  auth = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

### PROVIDERS

  prov1 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  prov2 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  prov3 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

### MIXES

  mix1 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix2 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix3 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix4 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix5 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix6 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

}
