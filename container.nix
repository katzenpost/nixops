{

### AUTHORITY

  auth = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

### PROVIDERS

  prov1 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  prov2 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  prov3 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

### MIXES

  mix1 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix2 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix3 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix4 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix5 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mix6 = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

### MAILPROXIES

  mail1a = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mail1b = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mail2a = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

  mail3a = { config, pkgs, ... }: {
    deployment = {
      targetEnv = "container";
      container.host = "localhost";
    };
  };

}
